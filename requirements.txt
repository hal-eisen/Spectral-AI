# SpectralAI — Python dependencies
# Install: pip install -r requirements.txt

torch>=2.1.0
numpy>=1.24.0
transformers>=4.40.0
datasets>=2.0.0
accelerate>=0.28.0
safetensors>=0.3.0
scikit-learn>=1.0.0
tqdm>=4.50.0
matplotlib>=3.5.0
bitsandbytes>=0.41.0  # Optional: INT4/INT8 quantization for large MoE models
huggingface-hub>=0.20.0
